import tw from 'tailwind.macro';
import styled from '@emotion/styled';


const RenderPropsBlock = styled.div`${tw``}`;
const MousePostion = styled.p`${tw``}`;
const Heading = styled.p`${tw`text-xl font-bold`}`;

export {RenderPropsBlock,Heading,MousePostion};