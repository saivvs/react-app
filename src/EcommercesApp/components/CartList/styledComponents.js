import tw from 'tailwind.macro';
import styled from '@emotion/styled';


const CartListTag = styled.div`${tw`h-full`}`;
const AddProduct = styled.div`${tw`text-white text-sm flex justify-center items-center h-full`}`;

export {CartListTag,AddProduct};