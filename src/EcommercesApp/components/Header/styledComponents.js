import tw from 'tailwind.macro';
import styled from '@emotion/styled';


const  HeaderTag= styled.div`${tw`flex justify-between`}`;
const CountOfProducts = styled.div`${tw``}`;


export {HeaderTag,CountOfProducts};