import tw from 'tailwind.macro';
import styled from '@emotion/styled';


const CheckOutButtonTag = styled.button`${tw`text-white bottom-0 flex flex-col justify-center items-center border border-black text-center bg-black m-2 p-2 w-full`}`;

export {CheckOutButtonTag};