import tw from 'tailwind.macro';
import styled from '@emotion/styled';

const TodoAppTag = styled.div`${tw`bg-gray-200 h-screen`}`;
const NoDataFound = styled.div`${tw`text-2xl font-bold text-center`}`;

export  {TodoAppTag,NoDataFound};