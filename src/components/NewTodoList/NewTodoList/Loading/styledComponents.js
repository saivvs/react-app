import tw from 'tailwind.macro';
import styled from '@emotion/styled';


const LoadingTag = styled.div`${tw`text-xl text-center flex justify-center items-center`}`;
const LoadingImage = styled.img`${tw``}`;

export {LoadingTag,LoadingImage};