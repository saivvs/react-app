import tw from 'tailwind.macro';
import styled from '@emotion/styled';


const TodoTag = styled.div`${tw``}`;
const TodoCheckBox = styled.input `${tw``}`;
const TodoInput = styled.input `${tw`border`}`;
const RemoveButton = styled.button `${tw``}`;

export {TodoTag,TodoCheckBox,TodoInput,RemoveButton};