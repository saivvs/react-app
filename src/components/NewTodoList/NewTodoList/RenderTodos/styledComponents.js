import tw from 'tailwind.macro';
import styled from '@emotion/styled';

const TodoAppTag = styled.div`${tw`bg-gray-200 h-screen`}`;
const TodoAppSubTag = styled.div`${tw``}`;

export  {TodoAppTag,TodoAppSubTag};